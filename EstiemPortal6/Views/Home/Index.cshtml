@{
    ViewBag.Title = "Home Page";
}
<div class="hidden-xs row" style="position:relative; margin-bottom:20px;">
    @{ Html.RenderAction("_Carousel"); }
</div>
<div class="event-preview row">
    <div class="col-md-6">
        <h2>Upcoming Events</h2>
        @{ Html.RenderAction("_EventShort"); }
    </div>
    <div class="col-md-6">
        <h2>Your Network</h2>
        @{ Html.RenderAction("_FriendsList"); }
    </div>

</div>