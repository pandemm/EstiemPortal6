@{
    ViewBag.Title = "Home Page";
}

<div>
    @{ Html.RenderAction("_Search"); }
</div>
<div class="event-preview">
    <div class="col-md-6">
        <h2>Upcoming Events</h2>     
        @{ Html.RenderAction("_EventShort"); }
    </div>
    <div class="col-md-6">
        <h2>Your Network</h2>
        @{ Html.RenderAction("_FriendsList"); }
    </div>

</div>